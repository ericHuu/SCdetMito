% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/atlas_benchmark_manifest.R
\name{atlas_benchmark_manifest}
\alias{atlas_benchmark_manifest}
\title{atlas_benchmark_manifest: curated benchmark datasets for broader validation}
\usage{
atlas_benchmark_manifest(role = NULL, species = NULL, tissue = NULL)
}
\arguments{
\item{role}{Optional role filter. Defaults to \code{NULL}, which returns all rows.}

\item{species}{Optional species filter. Defaults to \code{NULL}.}

\item{tissue}{Optional tissue filter. Defaults to \code{NULL}.}
}
\value{
A data frame describing recommended benchmark candidates.
}
\description{
\code{atlas_benchmark_manifest()} returns a curated table of public scRNA-seq or
snRNA-seq datasets that can be considered for validating \code{SCdetMito} across
authoritative lung cohorts, multi-organ atlases, and cross-species studies.
}
\details{
The manifest is intended to support manuscript planning and reproducible
validation design. It distinguishes between:
\itemize{
\item \code{main_text}: strong candidates for the primary paper benchmark
\item \code{independent_validation}: additional cohorts for replication
\item \code{supplementary_extension}: broader atlas studies for cross-species or
multi-tissue generalization
}

This is a reference manifest only. Returning a row does not validate current
dataset availability, download completeness, annotation quality, or local
reusability. Users must independently verify accessions, license terms,
data integrity, and analysis-ready input files before using any dataset for
manuscript claims.

\code{accession_or_portal} may be a GEO/BioProject accession, a portal name, or
\code{NA} when the source is known but the exact packaged input path is left to a
local curation step.
}
