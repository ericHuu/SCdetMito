% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_seu.R
\name{check_seu}
\alias{check_seu}
\title{check_seu: validate that a metadata column exists in a Seurat object}
\usage{
check_seu(
  seurat_obj,
  check = "mitoRatio",
  normalize_fraction = FALSE,
  must_be_numeric = FALSE
)
}
\arguments{
\item{seurat_obj}{A Seurat object.}

\item{check}{Metadata column name to validate. Defaults to \code{"mitoRatio"}.}

\item{normalize_fraction}{Whether to rescale percentage-like values to
fractions in the 0-1 range. Defaults to \code{FALSE}.}

\item{must_be_numeric}{Whether the target metadata column must be numeric.
Defaults to \code{FALSE}.}
}
\value{
The input Seurat object, potentially with a normalized metadata
column.
}
\description{
\code{check_seu()} is exported as a small metadata validation utility for
advanced users and for backward compatibility with earlier SCdetMito
workflows. New mitochondrial-ratio workflows should prefer
\code{\link[=ensure_mito_ratio]{ensure_mito_ratio()}} when the target column is mitochondrial content.
}
\examples{
# DO NOT RUN
# library(Seurat)
# seu <- CreateSeuratObject(matrix(rpois(2000, 5), nrow = 100))
# seu$mitoRatio <- runif(ncol(seu))
# check_seu(seu, "mitoRatio", normalize_fraction = TRUE)
# DO NOT RUN
}
