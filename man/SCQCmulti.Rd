% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SCQCmulti.R
\name{SCQCmulti}
\alias{SCQCmulti}
\title{SCQCmulti: perform QC for multi-sample single-cell RNA-seq data}
\usage{
SCQCmulti(
  seurat_obj,
  sample_col = NULL,
  group_col = NULL,
  by = NULL,
  group_by = NULL,
  mode = c("split", "all"),
  cutoff_strategy = c("consensus", "strictest", "groupwise"),
  cutoff_quantile = 0.25,
  cutoff_support_fraction = 0.5,
  scdet_options = list(),
  feature_col = NULL,
  count_col = NULL,
  mito_col = NULL,
  nFeature_RNA = "nFeature_RNA",
  nCount_RNA = "nCount_RNA",
  mitoRatio = "mitoRatio",
  min_genes = 200,
  max_genes = Inf,
  min_counts = 500,
  max_counts = Inf,
  min_mito = 0,
  max_mito = "SCdetMito",
  remove_doublets = NULL,
  removeDouble = FALSE,
  write_plots = NULL,
  plot = TRUE,
  write_tables = NULL,
  table_out = TRUE,
  output_dir = ".",
  fail_action = c("warn", "stop"),
  species = NULL,
  tissue = NULL,
  reference_cutoff = NULL,
  reference_table = NULL,
  reference_warning = TRUE,
  auto_add_mito = TRUE,
  mito_features = NULL,
  mito_pattern = NULL,
  mito_assay = NULL,
  recompute_mito = FALSE,
  use_recommended_cutoff = TRUE,
  ...
)
}
\arguments{
\item{seurat_obj}{A Seurat object containing single-cell RNA-seq data.}

\item{sample_col, by}{Metadata column identifying samples. For multi-group
studies this should be the sample column. \code{sample_col} is preferred; \code{by}
is retained for backward compatibility.}

\item{group_col, group_by}{Optional metadata column defining higher-level
biological groups that contain multiple samples. \code{group_col} is preferred.}

\item{mode}{QC strategy. \code{"split"} runs QC per sample and merges the
results; \code{"all"} runs QC on the full object using cell-level cutoffs
derived from the selected strategy.}

\item{cutoff_strategy}{Strategy used when \code{max_mito = "SCdetMito"}.
\code{"consensus"} derives sample-level cutoffs, selects one all-sample
sample-supported global cutoff for the integrated object, and applies it
across cells even when \code{group_col}/\code{group_by} is provided.
\code{"strictest"} applies the minimum sample-level cutoff across all samples.
\code{"groupwise"} is the only strategy that applies group-specific cutoffs, or
sample-specific cutoffs when \code{group_by = NULL}; the reported final cutoff
is the largest group-level supported cutoff.}

\item{cutoff_quantile}{Quantile used to summarize sample-level cutoffs within
a group for legacy workflows. Defaults to \code{0.25}.}

\item{cutoff_support_fraction}{Minimum fraction of samples required to support
a global or group-level recommended cutoff. A sample supports a cutoff when
its sample-level detected cutoff is at least that value. Defaults to \code{0.5}.}

\item{scdet_options}{Optional list of additional arguments forwarded to
\code{\link[=SCdetMito]{SCdetMito()}} when \code{max_mito = "SCdetMito"}. This can be used to select
alternative loss tests, multiple-testing correction methods, and
sample-level cutoff rules.}

\item{feature_col, nFeature_RNA}{Metadata column storing detected feature
counts. \code{feature_col} is preferred.}

\item{count_col, nCount_RNA}{Metadata column storing UMI/count totals.
\code{count_col} is preferred.}

\item{mito_col, mitoRatio}{Metadata column storing mitochondrial ratios.
\code{mito_col} is preferred.}

\item{min_genes}{Minimum number of detected genes. Defaults to \code{200}.}

\item{max_genes}{Maximum number of detected genes. Defaults to \code{Inf}.}

\item{min_counts}{Minimum number of counts. Defaults to \code{500}.}

\item{max_counts}{Maximum number of counts. Defaults to \code{Inf}.}

\item{min_mito}{Minimum mitochondrial ratio. Defaults to \code{0}. Values between
\code{1} and \code{100} are interpreted as percentages and converted to fractions.}

\item{max_mito}{Maximum mitochondrial ratio. Use \code{"SCdetMito"} to infer a
cutoff automatically. Numeric values between \code{1} and \code{100} are interpreted
as percentages and converted to fractions.}

\item{remove_doublets, removeDouble}{Whether to run optional
DoubletFinder-based doublet removal after QC filtering. \code{remove_doublets}
is preferred. Defaults to \code{FALSE}.}

\item{write_plots, plot}{Whether to export QC plots. \code{write_plots} is
preferred. Defaults to \code{TRUE}.}

\item{write_tables, table_out}{Whether to export QC summary tables.
\code{write_tables} is preferred. Defaults to \code{TRUE}.}

\item{output_dir}{Directory for exported files. Defaults to \code{"."}.}

\item{fail_action}{What to do if optional DoubletFinder removal fails.
\code{"warn"} returns the mito-QC-filtered object with a warning; \code{"stop"}
raises an error.}

\item{species, tissue}{Optional species and tissue names forwarded to
\code{\link[=SCdetMito]{SCdetMito()}} when adaptive mitochondrial cutoffs are requested.}

\item{reference_cutoff, reference_table, reference_warning}{Optional reference
cutoff controls forwarded to \code{\link[=SCdetMito]{SCdetMito()}}.}

\item{auto_add_mito}{Whether to calculate \code{mito_col} with
\code{\link[=ensure_mito_ratio]{ensure_mito_ratio()}} when the column is absent. Defaults to \code{TRUE}.}

\item{mito_features}{Optional complete mitochondrial feature vector forwarded
to \code{\link[=ensure_mito_ratio]{ensure_mito_ratio()}} and \code{\link[=SCdetMito]{SCdetMito()}}.}

\item{mito_pattern}{Optional mitochondrial feature regex forwarded to
\code{\link[=ensure_mito_ratio]{ensure_mito_ratio()}} and \code{\link[=SCdetMito]{SCdetMito()}}.}

\item{mito_assay}{Optional assay used for mitochondrial ratio calculation.}

\item{recompute_mito}{Whether to recompute \code{mito_col} even when it already
exists. Defaults to \code{FALSE}.}

\item{use_recommended_cutoff}{When \code{max_mito = "SCdetMito"}, use
\code{recommended_cutoff} from \code{\link[=SCdetMito]{SCdetMito()}} as the sample-level input for
consensus, strictest, and groupwise filtering. Defaults to \code{TRUE}. Set to
\code{FALSE} to apply the user-selected \code{selected_cutoff}.}

\item{...}{Additional parameters, currently unused.}
}
\value{
A QC-filtered Seurat object.
}
\description{
\code{SCQCmulti()} performs quality control for integrated multi-sample scRNA-seq
datasets. It supports both sample-wise QC and full-object QC with
sample-level or group-level mitochondrial cutoffs.
}
\details{
The function is designed for studies in which several samples may belong to
one biological group. When \code{max_mito = "SCdetMito"}, sample-level cutoffs
are first estimated and then aggregated according to the selected
\code{cutoff_strategy}.
}
\examples{
# DO NOT RUN
# library(Seurat)
# counts <- matrix(rpois(4000, 5), nrow = 100)
# mt_genes <- c("MT-ND1", "MT-ND2", "MT-CO1", "MT-CO2")
# # Representative toy mitochondrial genes only; not a complete gene set.
# rownames(counts) <- c(mt_genes, paste0("Gene", seq_len(96)))
# seu <- CreateSeuratObject(counts)
# seu$sample <- rep(c("A", "B"), each = ncol(seu) / 2)
# seu <- ensure_mito_ratio(seu, species = "human")
# qc_seu <- SCQCmulti(seu, by = "sample", mode = "split",
#   max_mito = 0.2, removeDouble = FALSE, plot = FALSE
# )
# qc_seu
# DO NOT RUN
}
