% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SCQCone.R
\name{SCQCone}
\alias{SCQCone}
\title{SCQCone: perform QC for a single single-cell RNA-seq sample or group}
\usage{
SCQCone(
  seurat_obj,
  feature_col = NULL,
  count_col = NULL,
  mito_col = NULL,
  nFeature_RNA = "nFeature_RNA",
  nCount_RNA = "nCount_RNA",
  mitoRatio = "mitoRatio",
  min_genes = 200,
  max_genes = Inf,
  min_counts = 500,
  max_counts = Inf,
  min_mito = 0,
  max_mito = "SCdetMito",
  remove_doublets = NULL,
  removeDouble = FALSE,
  write_plots = NULL,
  plot = TRUE,
  write_tables = NULL,
  table_out = TRUE,
  output_dir = ".",
  fail_action = c("warn", "stop"),
  species = NULL,
  tissue = NULL,
  reference_cutoff = NULL,
  reference_table = NULL,
  reference_warning = TRUE,
  auto_add_mito = TRUE,
  mito_features = NULL,
  mito_pattern = NULL,
  mito_assay = NULL,
  recompute_mito = FALSE,
  use_recommended_cutoff = TRUE,
  ...
)
}
\arguments{
\item{seurat_obj}{A Seurat object containing single-cell RNA-seq data for a
single sample.}

\item{feature_col, nFeature_RNA}{Metadata column storing detected feature
counts. \code{feature_col} is preferred; \code{nFeature_RNA} is retained for
backward compatibility.}

\item{count_col, nCount_RNA}{Metadata column storing UMI/count totals.
\code{count_col} is preferred.}

\item{mito_col, mitoRatio}{Metadata column storing mitochondrial ratios.
\code{mito_col} is preferred.}

\item{min_genes}{Minimum number of detected genes. Defaults to \code{200}.}

\item{max_genes}{Maximum number of detected genes. Defaults to \code{Inf}.}

\item{min_counts}{Minimum number of counts. Defaults to \code{500}.}

\item{max_counts}{Maximum number of counts. Defaults to \code{Inf}.}

\item{min_mito}{Minimum mitochondrial ratio. Defaults to \code{0}. Values between
\code{1} and \code{100} are interpreted as percentages and converted to fractions.}

\item{max_mito}{Maximum mitochondrial ratio. Use \code{"SCdetMito"} to infer a
cutoff automatically. Numeric values between \code{1} and \code{100} are interpreted
as percentages and converted to fractions.}

\item{remove_doublets, removeDouble}{Whether to run optional
DoubletFinder-based doublet removal after QC filtering. \code{remove_doublets}
is preferred. Defaults to \code{FALSE}.}

\item{write_plots, plot}{Whether to export QC plots. \code{write_plots} is
preferred. Defaults to \code{TRUE}.}

\item{write_tables, table_out}{Whether to export QC summary tables.
\code{write_tables} is preferred. Defaults to \code{TRUE}.}

\item{output_dir}{Directory for exported files. Defaults to \code{"."}.}

\item{fail_action}{What to do if optional DoubletFinder removal fails.
\code{"warn"} returns the mito-QC-filtered object with a warning; \code{"stop"}
raises an error.}

\item{species, tissue}{Optional species and tissue names forwarded to
\code{\link[=SCdetMito]{SCdetMito()}} when \code{max_mito = "SCdetMito"} for reference-aware cutoff
interpretation.}

\item{reference_cutoff, reference_table, reference_warning}{Optional reference
cutoff controls forwarded to \code{\link[=SCdetMito]{SCdetMito()}}.}

\item{auto_add_mito}{Whether to calculate \code{mito_col} with
\code{\link[=ensure_mito_ratio]{ensure_mito_ratio()}} when the column is absent. Defaults to \code{TRUE}.}

\item{mito_features}{Optional complete mitochondrial feature vector forwarded
to \code{\link[=ensure_mito_ratio]{ensure_mito_ratio()}} and \code{\link[=SCdetMito]{SCdetMito()}}.}

\item{mito_pattern}{Optional mitochondrial feature regex forwarded to
\code{\link[=ensure_mito_ratio]{ensure_mito_ratio()}} and \code{\link[=SCdetMito]{SCdetMito()}}.}

\item{mito_assay}{Optional assay used for mitochondrial ratio calculation.}

\item{recompute_mito}{Whether to recompute \code{mito_col} even when it already
exists. Defaults to \code{FALSE}.}

\item{use_recommended_cutoff}{When \code{max_mito = "SCdetMito"}, apply the
\code{recommended_cutoff} reported by \code{\link[=SCdetMito]{SCdetMito()}} instead of the
user-selected \code{selected_cutoff}. Defaults to \code{TRUE}.}

\item{...}{Additional parameters, currently unused.}
}
\value{
A QC-filtered Seurat object.
}
\description{
\code{SCQCone()} performs quality control for a single Seurat object using
user-supplied thresholds or a mitochondrial cutoff inferred by \code{SCdetMito()}.
}
\details{
The function summarizes cell counts before and after QC, optionally exports
QC plots and tables, and can optionally run DoubletFinder-based doublet
filtering after the primary mitochondrial, gene-count, and UMI-count filters
are applied. Doublet removal is disabled by default because it is not part of
the core mitochondrial cutoff detector and requires the optional
\code{DoubletFinder} package.
}
\examples{
# DO NOT RUN
# library(Seurat)
# counts <- matrix(rpois(2000, 5), nrow = 100)
# mt_genes <- c("MT-ND1", "MT-ND2", "MT-CO1", "MT-CO2")
# # Representative toy mitochondrial genes only; not a complete gene set.
# rownames(counts) <- c(mt_genes, paste0("Gene", seq_len(96)))
# seu <- CreateSeuratObject(counts)
# seu <- ensure_mito_ratio(seu, species = "human")
# qc_seu <- SCQCone(seu, max_mito = 0.2, removeDouble = FALSE, plot = FALSE)
# qc_seu
# DO NOT RUN
}
