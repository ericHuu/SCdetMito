% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SCdetMito.R
\name{SCdetMito}
\alias{SCdetMito}
\title{SCdetMito: retention-loss enrichment-based mitochondrial cutoff detection}
\usage{
SCdetMito(
  seurat_obj,
  mito_col = NULL,
  sample_col = NULL,
  mitoRatio = "mitoRatio",
  by = "sample",
  bin_width = 0.01,
  min_cut = 0.01,
  max_cut = 1,
  write_tables = NULL,
  write_plots = NULL,
  table_out = FALSE,
  plot = TRUE,
  min_drop_cells = NULL,
  diff_num = 5,
  min_drop_fraction = 0.01,
  min_cells_after = 20,
  min_retention_after = 0.1,
  min_sample_support = NULL,
  min_group_support = 0.5,
  loss_test = c("mad_zscore", "empirical_tail", "poisson_tail", "zscore",
    "threshold_only"),
  p_adj_method = NULL,
  p_adjust_method = "BH",
  alpha = 0.05,
  sample_cutoff_method = c("largest_drop", "max_significant", "median_significant",
    "min_significant"),
  output_dir = ".",
  fallback_method = c("quantile", "max_cut", "none"),
  fallback_quantile = 0.9,
  return_details = FALSE,
  ...
)
}
\arguments{
\item{seurat_obj}{A Seurat object.}

\item{mito_col, mitoRatio}{Metadata column storing mitochondrial ratios.
\code{mito_col} is the preferred name; \code{mitoRatio} is retained for backward
compatibility.}

\item{sample_col, by}{Metadata column identifying samples. \code{sample_col} is the
preferred name; \code{by} is retained for backward compatibility.}

\item{bin_width}{Candidate cutoff grid step size.}

\item{min_cut, max_cut}{Minimum and maximum mitochondrial ratio cutoffs
considered.}

\item{write_tables, table_out}{Whether to export retained-cell and interval
loss tables. \code{write_tables} is preferred.}

\item{write_plots, plot}{Whether to export manuscript-ready PDF plots.
\code{write_plots} is preferred.}

\item{min_drop_cells, diff_num}{Minimum absolute interval-specific cell loss.
\code{min_drop_cells} is preferred; \code{diff_num} is retained for compatibility.}

\item{min_drop_fraction}{Minimum interval loss fraction relative to the
sample cell count.}

\item{min_cells_after}{Minimum retained cells after the candidate cutoff.}

\item{min_retention_after}{Minimum retained fraction after the candidate
cutoff.}

\item{min_sample_support, min_group_support}{Minimum fraction of samples that
must support a global cutoff. \code{min_sample_support} is preferred.}

\item{loss_test}{Retention-loss enrichment test. Defaults to
\code{"mad_zscore"} as the primary robust FDR-compatible detector. Other
options are \code{"empirical_tail"}, \code{"poisson_tail"}, \code{"zscore"}, and
\code{"threshold_only"}.}

\item{p_adj_method, p_adjust_method}{Multiple-testing correction method passed
to \code{\link[stats:p.adjust]{stats::p.adjust()}}. Use \code{"none"} to leave p-values unadjusted.
\code{p_adj_method} is preferred.}

\item{alpha}{Adjusted p-value threshold. Defaults to \code{0.05}.}

\item{sample_cutoff_method}{Rule for selecting one sample-level cutoff from
significant intervals. Defaults to \code{"largest_drop"}.}

\item{output_dir}{Directory for exported outputs.}

\item{fallback_method}{Fallback used only when no significant interval is
found for a sample. Options are \code{"quantile"}, \code{"max_cut"}, and \code{"none"}.}

\item{fallback_quantile}{Quantile used when \code{fallback_method = "quantile"}.}

\item{return_details}{Whether to return detailed tables. Defaults to \code{FALSE}.}

\item{...}{Additional arguments ignored for backward compatibility.}
}
\value{
A numeric sample-supported global cutoff, or a list with detailed
retained-cell profiles, interval-loss tables, significant intervals, and
settings when \code{return_details = TRUE}. The \code{change_points} element is
retained as a legacy alias of \code{interval_loss_table}.
}
\description{
\code{SCdetMito()} estimates sample-aware mitochondrial ratio cutoffs from
retained-cell profiles. The function is kept under its original public name
for backward compatibility, but the statistical framing is retention-loss
enrichment-based adaptive mitochondrial QC rather than a breakpoint model.
}
\details{
For each sample \code{s} and candidate mitochondrial cutoff \code{c}, the retained-cell
profile is calculated as \code{R_s(c) = sum(m_i < c)}, where \code{m_i} is the
mitochondrial ratio of cell \code{i}. Adjacent candidate cutoffs define an
interval-specific cell loss, \verb{L_s(c_j) = R_s(c_\{j-1\}) - R_s(c_j)}. Candidate
intervals are first filtered by absolute cell loss, relative cell loss,
retained cell count, and retained fraction. Retention-loss enrichment is then
evaluated within each sample, p-values are adjusted per sample, and
significant intervals are summarized into sample-level cutoff candidates.
A sample-supported global cutoff is returned for multi-sample inputs.
Confidence labels describe QC decision confidence only: high/moderate labels
require a statistically detected, non-fallback interval, whereas low labels
mark fallback-derived or not-detected cutoffs.
}
