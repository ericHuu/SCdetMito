% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_mitoRatio.R
\name{ensure_mito_ratio}
\alias{ensure_mito_ratio}
\title{Ensure mitochondrial ratio metadata are available}
\usage{
ensure_mito_ratio(
  object,
  mito_col = "mitoRatio",
  species = NULL,
  assay = NULL,
  mito_features = NULL,
  mito_pattern = NULL,
  overwrite = FALSE,
  convert_percent = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{object}{A Seurat object.}

\item{mito_col}{Metadata column used for mitochondrial fractions. Defaults to
\code{"mitoRatio"}.}

\item{species}{Optional species preset used for mitochondrial feature
detection.}

\item{assay}{Assay used to identify features and calculate percentages.
Defaults to the active assay.}

\item{mito_features}{Optional complete character vector of mitochondrial
features retained in the object.}

\item{mito_pattern}{Optional regular expression used to identify
mitochondrial features.}

\item{overwrite}{Whether to recalculate \code{mito_col} when it already exists.
Defaults to \code{FALSE}.}

\item{convert_percent}{Whether percent-scale existing values should be
converted to fractions. Defaults to \code{TRUE}.}

\item{verbose}{Whether to emit status messages. Defaults to \code{TRUE}.}
}
\value{
A Seurat object with \code{object@meta.data[[mito_col]]} stored as a
numeric mitochondrial fraction and summary information in
\code{object@misc$SCdetMito_mito_ratio_info}.
}
\description{
\code{ensure_mito_ratio()} is the unified mitochondrial fraction preparation
function used by SCdetMito workflows. It validates an existing mitochondrial
metadata column or calculates one from mitochondrial features when possible.
}
\details{
Values are stored as fractions on a 0--1 scale. If an existing column appears
to be percent-scale and \code{convert_percent = TRUE}, it is converted to a
fraction. When the column is absent, mitochondrial features are detected from
user-supplied \code{mito_features}, a user-supplied \code{mito_pattern}, species-aware
gene-symbol patterns, or feature metadata columns such as \code{gene_name},
\code{gene_short_name}, \code{symbol}, \code{feature_name}, and \code{name}.

If mitochondrial features were removed during preprocessing, the ratio cannot
be reconstructed from the object alone. In that case, provide a valid
\code{mito_features} vector or a precomputed \code{mito_col} from raw counts that
retained mitochondrial features.
}
\examples{
seu <- load_demo_pbmc()
seu <- ensure_mito_ratio(seu, mito_col = "mitoRatio")

mito_features <- grep("^MT-", rownames(seu), value = TRUE)
if (length(mito_features)) {
  seu <- ensure_mito_ratio(seu, mito_features = mito_features)
}
}
