% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/public_data_loaders.R
\name{load_mousebrain5k_online}
\alias{load_mousebrain5k_online}
\title{Load a public 10x Genomics mouse brain 5k nuclei dataset}
\usage{
load_mousebrain5k_online(
  cache_dir = tools::R_user_dir("SCdetMito", which = "cache"),
  add_mito = TRUE,
  mito_col = "mitoRatio",
  min_cells = 3,
  min_features = 200,
  url = NULL,
  local_file = NULL,
  verbose = TRUE
)
}
\arguments{
\item{cache_dir}{Directory used to cache the downloaded 10x HDF5 matrix.
Defaults to the user cache directory for SCdetMito.}

\item{add_mito}{Whether to add mitochondrial ratio metadata when \code{mito_col}
is absent. Defaults to \code{TRUE}.}

\item{mito_col}{Metadata column used for mitochondrial ratios. Defaults to
\code{"mitoRatio"}.}

\item{min_cells}{Minimum cells per feature passed to
\code{\link[Seurat:reexports]{Seurat::CreateSeuratObject()}}.}

\item{min_features}{Minimum detected features per cell passed to
\code{\link[Seurat:reexports]{Seurat::CreateSeuratObject()}}.}

\item{url}{Optional URL overriding the built-in public 10x Genomics HDF5 URL.}

\item{local_file}{Optional path to a manually downloaded filtered
feature-barcode HDF5 file. When supplied, no download is attempted.}

\item{verbose}{Whether to print cache and download messages. Defaults to
\code{TRUE}.}
}
\value{
A Seurat object containing public mouse brain nuclei with
mitochondrial metadata when \code{add_mito = TRUE}.
}
\description{
This optional helper loads the public 10x Genomics 5k adult mouse brain
nuclei dataset isolated with the Chromium Nuclei Isolation Kit. The dataset
is not included in SCdetMito and is downloaded only when this function is
called. It is intended as a real tissue/nuclei candidate for dataset
scouting before selecting SoftwareX figures.
}
\details{
Internet access is required unless the 10x HDF5 file has already been
cached. Nuclei datasets may have low mitochondrial fractions, so this loader
should be treated as a dataset-scouting candidate rather than a guaranteed
high-mitochondrial example.
}
\examples{
\dontrun{
brain <- load_mousebrain5k_online()
det <- SCdetMito(brain, sample_col = "sample", mito_col = "mitoRatio")
}
}
