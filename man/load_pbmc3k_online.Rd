% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/public_data_loaders.R
\name{load_pbmc3k_online}
\alias{load_pbmc3k_online}
\title{Load the public PBMC3K dataset for SCdetMito examples}
\usage{
load_pbmc3k_online(
  method = c("SeuratData", "10x"),
  cache_dir = tools::R_user_dir("SCdetMito", which = "cache"),
  add_mito = TRUE,
  mito_col = "mitoRatio",
  min_cells = 3,
  min_features = 200,
  verbose = TRUE
)
}
\arguments{
\item{method}{Loading method. \code{"SeuratData"} loads an already installed
SeuratData PBMC3K object. \code{"10x"} downloads the public 10x Genomics
filtered gene-barcode matrix to \code{cache_dir}.}

\item{cache_dir}{Directory used to cache the downloaded 10x archive and
extracted matrix. Defaults to the user cache directory for SCdetMito.}

\item{add_mito}{Whether to add mitochondrial ratio metadata when \code{mito_col}
is absent. Defaults to \code{TRUE}.}

\item{mito_col}{Metadata column used for mitochondrial ratios. Defaults to
\code{"mitoRatio"}.}

\item{min_cells}{Minimum cells per feature passed to
\code{\link[Seurat:reexports]{Seurat::CreateSeuratObject()}} for the 10x method.}

\item{min_features}{Minimum detected features per cell passed to
\code{\link[Seurat:reexports]{Seurat::CreateSeuratObject()}} for the 10x method.}

\item{verbose}{Whether to print cache and download messages. Defaults to
\code{TRUE}.}
}
\value{
A Seurat object containing the public PBMC3K data with minimal
metadata and mitochondrial metadata when \code{add_mito = TRUE}.
}
\description{
This optional helper loads the public 10x Genomics PBMC3K dataset either
through SeuratData or by downloading the filtered gene-barcode matrix from
the public 10x Genomics URL. PBMC3K is provided as a small public
smoke-test example. Because PBMCs often have relatively low mitochondrial
fractions and dataset-specific QC structure, PBMC3K cutoff results should be
interpreted as a functionality check rather than evidence of a universal
mitochondrial threshold.
}
\details{
This function is intended for explicit real-data smoke tests. It is not used
during package installation, examples, automated tests, or R CMD check.
Internet access is required for \code{method = "10x"} unless the 10x archive has
already been cached.
}
\examples{
\dontrun{
pbmc <- load_pbmc3k_online(method = "10x")
det <- SCdetMito(pbmc, sample_col = "sample", mito_col = "mitoRatio")
}
}
