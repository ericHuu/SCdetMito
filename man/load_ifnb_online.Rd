% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/public_data_loaders.R
\name{load_ifnb_online}
\alias{load_ifnb_online}
\title{Load the public IFNB-stimulated/control PBMC metadata example}
\usage{
load_ifnb_online(
  add_mito = TRUE,
  mito_col = "mitoRatio",
  sample_col = "sample",
  group_col = "condition",
  install_if_missing = interactive(),
  verbose = TRUE
)
}
\arguments{
\item{add_mito}{Whether to add mitochondrial ratio metadata when \code{mito_col}
is absent. Defaults to \code{TRUE}.}

\item{mito_col}{Metadata column used for mitochondrial ratios. Defaults to
\code{"mitoRatio"}.}

\item{sample_col}{Metadata column to create or harmonize for sample-level
SCdetMito workflows. Defaults to \code{"sample"}.}

\item{group_col}{Metadata column to create or harmonize for condition-level
workflows. Defaults to \code{"condition"}.}

\item{install_if_missing}{Whether to install the IFNB dataset with
\code{SeuratData::InstallData("ifnb")} when SeuratData is available but the
dataset is missing. Defaults to \code{\link[=interactive]{interactive()}}.}

\item{verbose}{Whether to print loading messages. Defaults to \code{TRUE}.}
}
\value{
A Seurat object with harmonized sample, condition, dataset, tissue,
and species metadata. Mitochondrial metadata are added only when
\code{add_mito = TRUE} and mitochondrial features are available.
}
\description{
This optional helper loads the public IFNB-stimulated and control PBMC
dataset from SeuratData. It is intended as a multi-condition metadata
example for testing sample and group harmonization. The processed SeuratData
object may not retain mitochondrial genes and may not include a usable
\code{mitoRatio} or \code{percent.mt} metadata column, so it should not be treated as a
guaranteed mitochondrial QC example. The dataset is not included in
SCdetMito.
}
\details{
SeuratData is used as an optional dependency. The IFNB dataset can be
installed on request with \code{install_if_missing = TRUE}; this is never done
during package checks unless users explicitly call the function that way.
Processed IFNB objects may not retain mitochondrial features. In that case,
load the object with \code{add_mito = FALSE} and provide raw counts retaining
mitochondrial genes or a valid precomputed mitochondrial fraction column
before running SCdetMito workflows.
}
\examples{
\dontrun{
ifnb <- load_ifnb_online(add_mito = FALSE)

if ("mitoRatio" \%in\% colnames(ifnb@meta.data)) {
  qc <- SCQCmulti(
    ifnb,
    sample_col = "sample",
    group_col = "condition",
    mito_col = "mitoRatio",
    cutoff_strategy = "groupwise"
  )
} else {
  message(
    "IFNB does not contain mitoRatio or mitochondrial features; ",
    "provide raw counts or a valid mito_col before running SCdetMito."
  )
}
}
}
