% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SCQCbenchmark.R
\name{SCQCbenchmark}
\alias{SCQCbenchmark}
\title{SCQCbenchmark: benchmark fixed and adaptive QC strategies}
\usage{
SCQCbenchmark(
  seurat_obj,
  sample_by = NULL,
  group_by = NULL,
  sample_col = NULL,
  group_col = NULL,
  strategies = NULL,
  scdet_options = list(),
  nFeature_RNA = "nFeature_RNA",
  nCount_RNA = "nCount_RNA",
  mitoRatio = "mitoRatio",
  feature_col = NULL,
  count_col = NULL,
  mito_col = NULL,
  min_genes = 200,
  max_genes = Inf,
  min_counts = 500,
  max_counts = Inf,
  min_mito = 0,
  remove_doublets = NULL,
  removeDouble = FALSE,
  run_downstream = TRUE,
  cluster_resolution = 0.5,
  npcs = 20,
  seed = 123,
  score_weights = NULL,
  scoring_profiles = c("balanced", "retention_focused", "stringent_mito",
    "balance_focused"),
  species = NULL,
  tissue = NULL,
  reference_cutoff = NULL,
  reference_table = NULL,
  reference_warning = TRUE,
  auto_add_mito = TRUE,
  mito_features = NULL,
  mito_pattern = NULL,
  mito_assay = NULL,
  recompute_mito = FALSE,
  write_plots = NULL,
  plot = TRUE,
  save_objects = FALSE,
  output_dir = "."
)
}
\arguments{
\item{seurat_obj}{A Seurat object containing multi-sample single-cell data.}

\item{sample_by, sample_col}{Metadata column defining sample IDs.
\code{sample_col} is the clearer preferred name; \code{sample_by} is retained for
backward compatibility.}

\item{group_by, group_col}{Optional metadata column defining biological groups.
\code{group_col} is the clearer preferred name; \code{group_by} is retained for
backward compatibility.}

\item{strategies}{Optional list of strategy specifications. When \code{NULL}, a
default benchmark panel is used.}

\item{scdet_options}{Optional list of arguments forwarded to \code{\link[=SCdetMito]{SCdetMito()}}
for adaptive strategies. Individual strategies can override these values
by defining their own \code{scdet_options} entry.}

\item{nFeature_RNA, feature_col}{Metadata column storing detected feature
counts.}

\item{nCount_RNA, count_col}{Metadata column storing UMI/count totals.}

\item{mitoRatio, mito_col}{Metadata column storing mitochondrial ratios.}

\item{min_genes}{Minimum number of detected genes. Defaults to \code{200}.}

\item{max_genes}{Maximum number of detected genes. Defaults to \code{Inf}.}

\item{min_counts}{Minimum number of counts. Defaults to \code{500}.}

\item{max_counts}{Maximum number of counts. Defaults to \code{Inf}.}

\item{min_mito}{Minimum mitochondrial ratio. Defaults to \code{0}. Values between
\code{1} and \code{100} are interpreted as percentages and converted to fractions by
the underlying QC functions.}

\item{removeDouble, remove_doublets}{Whether to run optional DoubletFinder-
based doublet removal inside each benchmark strategy. Defaults to \code{FALSE}.}

\item{run_downstream}{Whether to run lightweight downstream clustering and
compute cluster-mixing diagnostics. Defaults to \code{TRUE}.}

\item{cluster_resolution}{Resolution used for Seurat clustering in downstream
benchmarking. Defaults to \code{0.5}.}

\item{npcs}{Maximum number of PCs used for downstream clustering. Defaults to
\code{20}.}

\item{seed}{Random seed for downstream analysis. Defaults to \code{123}.}

\item{score_weights}{Optional named numeric vector or list controlling a
custom composite score. When supplied, it is reported as the \code{custom}
scoring profile and used as the backward-compatible primary score.}

\item{scoring_profiles}{Built-in scoring profiles to compute. Available
profiles are \code{"balanced"}, \code{"retention_focused"}, \code{"stringent_mito"}, and
\code{"balance_focused"}.}

\item{species, tissue}{Optional species and tissue names forwarded to
\code{\link[=SCdetMito]{SCdetMito()}} for reference-aware adaptive strategies.}

\item{reference_cutoff, reference_table, reference_warning}{Optional reference
cutoff controls forwarded to \code{\link[=SCdetMito]{SCdetMito()}}.}

\item{auto_add_mito}{Whether to calculate \code{mito_col} with
\code{\link[=ensure_mito_ratio]{ensure_mito_ratio()}} when the column is absent. Defaults to \code{TRUE}.}

\item{mito_features}{Optional complete mitochondrial feature vector forwarded
to \code{\link[=ensure_mito_ratio]{ensure_mito_ratio()}} and adaptive SCdetMito strategies.}

\item{mito_pattern}{Optional mitochondrial feature regex forwarded to
\code{\link[=ensure_mito_ratio]{ensure_mito_ratio()}} and adaptive SCdetMito strategies.}

\item{mito_assay}{Optional assay used for mitochondrial ratio calculation.}

\item{recompute_mito}{Whether to recompute \code{mito_col} even when it already
exists. Defaults to \code{FALSE}.}

\item{plot, write_plots}{Whether to export benchmark plots. Defaults to
\code{TRUE}. \code{write_plots} is the clearer preferred name.}

\item{save_objects}{Whether to save strategy-specific filtered Seurat objects.
Defaults to \code{FALSE}.}

\item{output_dir}{Directory for benchmark outputs. Defaults to \code{"."}.}
}
\value{
Invisibly returns a list with raw benchmark tables, normalized score
tables, weights used, strategies recommended under each scoring profile,
retention tables, sample-level cutoff plans, and filtered objects. Legacy
aliases \code{summary}, \code{scores}, and \code{recommendation} are retained.
}
\description{
\code{SCQCbenchmark()} compares fixed mitochondrial thresholds with adaptive
\code{SCdetMito}-based strategies on the same multi-sample Seurat object.
}
\details{
The function is intended for method evaluation and manuscript preparation.
It summarizes retained cell numbers, residual mitochondrial burden,
sample/group balance, and lightweight downstream clustering diagnostics for
each QC strategy. Composite scores are user-adjustable decision-support
summaries and do not imply a universal QC optimum.
}
