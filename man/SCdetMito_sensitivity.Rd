% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SCdetMito_sensitivity.R
\name{SCdetMito_sensitivity}
\alias{SCdetMito_sensitivity}
\title{Sensitivity analysis for SCdetMito cutoff estimation}
\usage{
SCdetMito_sensitivity(
  seurat_obj,
  sample_col = NULL,
  by = "sample",
  mito_col = NULL,
  mitoRatio = "mitoRatio",
  bin_width = c(0.01, 0.02),
  alpha = c(0.01, 0.05, 0.1),
  min_drop_fraction = c(0.005, 0.01, 0.02),
  loss_test = c("mad_zscore", "empirical_tail"),
  sample_cutoff_method = c("largest_drop", "max_significant"),
  write_tables = FALSE,
  write_plots = FALSE,
  output_dir = ".",
  return_details = FALSE,
  ...
)
}
\arguments{
\item{seurat_obj}{A Seurat object containing single-cell RNA-seq QC metadata.}

\item{sample_col, by}{Metadata column defining sample IDs. \code{sample_col} is the
preferred name; \code{by} is retained for backward compatibility.}

\item{mito_col, mitoRatio}{Metadata column storing mitochondrial ratios.}

\item{bin_width}{Candidate cutoff grid step sizes to test.}

\item{alpha}{Adjusted p-value cutoffs to test.}

\item{min_drop_fraction}{Minimum interval-loss fractions to test.}

\item{loss_test}{Loss enrichment tests to evaluate. Defaults to
\code{"mad_zscore"} plus \code{"empirical_tail"} as a sensitivity comparison.
Supported values are \code{"mad_zscore"}, \code{"empirical_tail"},
\code{"poisson_tail"}, \code{"zscore"}, and \code{"threshold_only"}.}

\item{sample_cutoff_method}{Sample-level cutoff selection rules to evaluate.}

\item{write_tables}{Whether to write the combined sensitivity table.}

\item{write_plots}{Whether to write optional stability plots.}

\item{output_dir}{Output directory for optional tables and plots.}

\item{return_details}{Whether to return both the per-combination table and
sample-level sensitivity summary. Defaults to \code{FALSE}, which preserves the
legacy behavior of returning only the per-combination table.}

\item{...}{Additional arguments forwarded to \code{\link[=SCdetMito]{SCdetMito()}}.}
}
\value{
A data frame with one row per sample and parameter combination, or a
list with \code{sensitivity_table} and \code{sensitivity_summary} when
\code{return_details = TRUE}.
}
\description{
\code{SCdetMito_sensitivity()} evaluates whether sample-level mitochondrial
cutoffs are stable across user-specified retention-loss enrichment settings.
It is intended for method auditing, supplementary analyses, and manuscript
robustness checks.
}
