% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_mitoRatio.R
\name{add_mitoRatio}
\alias{add_mitoRatio}
\title{add_mitoRatio: backward-compatible mitochondrial ratio wrapper}
\usage{
add_mitoRatio(
  seurat_obj,
  pattern = "auto",
  features = NULL,
  species = NULL,
  assay = NULL,
  column = "mitoRatio",
  scale = c("fraction", "percent"),
  ignore_case = FALSE
)
}
\arguments{
\item{seurat_obj}{A Seurat object.}

\item{pattern}{Regex pattern used to match mitochondrial features. Use
\code{"auto"} to scan common mitochondrial prefixes. Defaults to \code{"auto"}.}

\item{features}{Optional complete character vector of mitochondrial feature
names. When supplied, \code{pattern} and \code{species} are ignored.}

\item{species}{Optional species preset used for mitochondrial feature
detection.}

\item{assay}{Assay used to calculate feature percentages. Defaults to the
active assay.}

\item{column}{Metadata column name used to store the result. Defaults to
\code{"mitoRatio"}.}

\item{scale}{Output scale for the stored metric. \code{"fraction"} stores values
between 0 and 1 and \code{"percent"} stores values between 0 and 100.}

\item{ignore_case}{Retained for backward compatibility. Matching now tries
case-sensitive and case-insensitive detection internally.}
}
\value{
The input Seurat object with an added mitochondrial ratio column in
\code{meta.data}.
}
\description{
\code{add_mitoRatio()} is retained for backward compatibility. New workflows can
use \code{\link[=ensure_mito_ratio]{ensure_mito_ratio()}} directly.
}
\details{
The function delegates mitochondrial feature detection and fraction
calculation to \code{\link[=ensure_mito_ratio]{ensure_mito_ratio()}}. Manual mitochondrial feature lists
should include all mitochondrial features retained in the object. Short
illustrative subsets should not be used for real QC.
}
