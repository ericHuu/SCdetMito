% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_mitoRatio.R
\name{add_mitoRatio}
\alias{add_mitoRatio}
\title{add_mitoRatio: calculate and store mitochondrial ratios in a Seurat object}
\usage{
add_mitoRatio(
  seurat_obj,
  pattern = "auto",
  features = NULL,
  species = NULL,
  assay = NULL,
  column = "mitoRatio",
  scale = c("fraction", "percent"),
  ignore_case = FALSE
)
}
\arguments{
\item{seurat_obj}{A Seurat object.}

\item{pattern}{Regex pattern used to match mitochondrial features. Use
\code{"auto"} to scan common mitochondrial prefixes. Defaults to \code{"auto"}.}

\item{features}{Optional character vector of mitochondrial feature names. When
supplied, \code{pattern} and \code{species} are ignored.}

\item{species}{Optional species preset used for mitochondrial feature
detection. Supported values are \code{"auto"}, \code{"human"}, \code{"mouse"}, \code{"rat"},
\code{"pig"}, \code{"cow"}, \code{"cattle"}, \code{"sheep"}, \code{"goat"}, \code{"chicken"},
\code{"duck"}, \code{"goose"}, and \code{"mixed"}. Defaults to \code{NULL}.}

\item{assay}{Assay used to calculate feature percentages. Defaults to the
active assay.}

\item{column}{Metadata column name used to store the result. Defaults to
\code{"mitoRatio"}.}

\item{scale}{Output scale for the stored metric. \code{"fraction"} stores values
between 0 and 1 and \code{"percent"} stores values between 0 and 100.}

\item{ignore_case}{Whether feature matching should ignore case when \code{pattern}
or \code{species}-based detection is used. Defaults to \code{FALSE}.}
}
\value{
The input Seurat object with an added mitochondrial ratio column in
\code{meta.data}.
}
\description{
\code{add_mitoRatio()} calculates mitochondrial abundance for each cell and stores
the result in \code{meta.data}.
}
\details{
By default the function uses \code{pattern = "auto"} and searches for common
mitochondrial prefixes such as \verb{MT-}, \verb{mt-}, and \verb{Mt-}. This makes the
function safer for public data reuse and cross-species workflows where gene
naming conventions vary. When \code{species} is supplied, \code{add_mitoRatio()}
additionally tries curated exact mitochondrial gene symbols for that species.
This is important for livestock datasets where mitochondrial genes are often
annotated as symbols such as \code{ND1}, \code{COX1}, \code{ATP6}, and \code{CYTB} rather than
\verb{MT-}-prefixed features.
}
